import React from "react";
import Nav from "./Nav";
import MainPage from "./MainPage";

const Hero = () => (
    <div className="hero">
        <Nav />
        <MainPage />
    </div>
)

export default Hero;
